import React from "react";
import CView from "../../ui/CView/CView";
type ClientsProps = {};
const Clients: React.FC<ClientsProps> = ({}) => {
	return (
		<CView height={3000} bgx="bg300">
			En Clientes
		</CView>
	);
};
export default Clients;
