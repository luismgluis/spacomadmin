import React from "react";
import CView from "../../ui/CView/CView";
type ConfigProps = {};
const Config: React.FC<ConfigProps> = ({}) => {
	return <CView py={20}>Hola estamos en config</CView>;
};
export default Config;
